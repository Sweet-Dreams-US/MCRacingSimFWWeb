<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required by Stripe Terminal. Keep permissions MINIMAL — anything outside
         Stripe's vetted set is rejected during app review. Location is mandatory:
         Terminal.init throws if location services are off. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".MyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MCRacingPOS"
        tools:targetApi="34">

        <!-- A normal LAUNCHER activity. Stripe assigns the kiosk/home role at
             deploy time (choose this as the "kiosk app" in the Dashboard). -->
        <!-- Locked to tall portrait. The reader is handheld and gets passed to
             the customer to tap and tip; letting it flip to landscape (or
             upside-down) mid-sale reflows the whole UI for no benefit.
             "portrait" is stricter than "sensorPortrait" — it also prevents the
             180-degree flip when the device is turned over.

             uiMode is in configChanges because docking/undocking a Stripe
             reader raises a configuration change: without it Android recreates
             the activity, which would wipe a half-entered sale mid-transaction.
             (Per Stripe's Apps on Devices "Working with device accessories".) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
